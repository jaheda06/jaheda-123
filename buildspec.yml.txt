version: 0.2

phases:
  install:
    commands:
      - echo Installing dependencies...
      - yum update -y
      - yum install -y docker

  pre_build:
    commands:
      - echo Logging into AWS ECR...
      - aws ecr get-login-password --region ap-south-1 | docker login --username AWS --password-stdin 556965128158.dkr.ecr.ap-south-1.amazonaws.com

  build:
    commands:
      - echo Building the Docker image...
      - docker build -t $ImageName .

  post_build:
    commands:
      - echo Tagging the Docker image...
      - docker tag $ImageName:latest 556965128158.dkr.ecr.ap-south-1.amazonaws.com/$ImageName:latest
      - echo Pushing the Docker image to ECR...
      - docker push 556965128158.dkr.ecr.ap-south-1.amazonaws.com/$ImageName:latest
